#include <stdio.h>
#define PI 3.14
int main (void) {
    double x=21.2;
    float res=8.0;
    double  k;
    printf("Enter the resistor value: ");
    scanf("%lf",k);
    double y;
    y=k*res;
    printf("y is: %f", y);

}
